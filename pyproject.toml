[project]
name = "cart-fsd"
version = "0.1.0"
description = "Self-driving golf cart control system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.6.1",
    "pyserial",
]
